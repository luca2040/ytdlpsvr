This is a simple server that allows you to download music (in mp3 format) through yt-dlp via a web interface.
I tried to make it the simplest possible.

Also i rushed this in around two days because i needed it, and so the code may be messy and the implementation not optimal.

This has been written in zig 0.16.0 so any other version may break things.
